\chapter{Results}
\section{Overview}
This chapter presents the validation process and results of the processes outlined in the 
Methodology (Chapter 
\ref{sec:methodology}). The results are organised by operation. Each section
discusses the correctness and computational performance of an algorithm. Additionally implementation 
decisions for each of the stages are compared for both their speed and accuracy. The results are 
presented as plots, and are accompanied by a discussion of their implications for the design 
of the radar processing pipeline in the following chapter. 

Before the per stage performance is presented, a wholistic view of the pipeline's performance is
given, including the tracking performance of the pipeline as a whole, and the contribution of each
stage to the overall latency of the pipeline.  

The pipeline demonstrates full real-time multi-target tracking functionality on an NVIDIA Xavier 
AGX on the DS2 and DS3 datasets, with a latency of less than 50ms while processing 4 video streams
of 256 chirps, each with 767 samples, per frame at a rate of 10 frames per second. On top of this,
the harness used for visualisation can also run online, allowing the user to see the correct 
behaviour of each of the stages of the pipeline in real time, (see Figures \ref{fig:fft-visual}, 
\ref{fig:cfar-visual}, \ref{fig:cluster-visual}, \ref{fig:angle-visual}, \ref{fig:track-visual1}, 
and \ref{fig:track-visual2}).

% Filenames: visual-fft-png, visual-angle.png, visual-cfar.png, visual-cluster.png, visual-track.png
\begin{figure}
    \centering
    \includegraphics[width=0.9\textwidth]{visual-fft.png} 
    \caption[Visualisation of the FFT stage.]{Visualisation of the FFT stage for visual
    validation. The y-axis is time and the x-axis is range. The colour is amplitude. With 
    yellow indicating highest amplitude. A constant range target appears to be present in 
    the right hand 
    side of the waterfall.}
    \label{fig:fft-visual}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.9\textwidth]{visual-cfar.png} 
    \caption[Visualisation of the CFAR stage.]{Visualisation of the CFAR stage for visual
    validation. The y-axis is time and the x-axis is range. The colour is amplitude
    and no colour (black) indicates that that range bin has a value below the 
    threshold.}
    \label{fig:cfar-visual}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=1\textwidth]{visual-cluster.png} 
    \caption[Visualisation of the clustering stage.]{Visualisation of the clustering 
    stage for visual
    validation. The y-axis is time and the x-axis is range. The colour is amplitude. 
    This time, range is a continuous variable instead of in discrete bins. 
    In this example, there is a \~ -40dB target at around 1 km surrounded 
    by a high level of -47dB clutter.}
    \label{fig:cluster-visual}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.9\textwidth]{visual-angle.png} 
    \caption[Visualisation of the angle sensing stage.]{Visualisation of the angle 
    sensing stage for visual
    validation. Depicted is a 3D plot of all detections accumulated over time.
    The colour yellow indicates high power detections and purple indicates 
    the lowest power detections. Depicted are two collections of points that
    correspond to targets that were once in those positions. }
    \label{fig:angle-visual}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{visual-track.png} 
    \caption[Visualisation of the tracking stage.]{A 3D plot containing all detections
    tentative tracks, confirmed tracks, and hypotheses accumulated over time with 
    playback controls. 
    stage for visual validation. 
    validation. Ellipses indicate the uncertainty of the element at its
    centre. For example, white white spheres indicate measurement 
    uncertainty, and yellow indicates track update uncertainty. The FOV 
    of each of the five quadruplets is depicted as coloured lines. The image 
    currently depicted a second of mostly clutter, with a cluster of 
    detections at around 1 km from the radar (middle of bottom third 
    of scene). }
    \label{fig:track-visual1}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.6\textwidth]{visual-track2.png}
    \caption[Tracker Visualisation Zoomed In]{A close up view of 
    the tracking stage identifying a target at around 1 km from the
    radar in high noise conditions. As the ground truth progresses 
    (turquoise line), a tentative track is born (red line, blue dot),
    is updated (yellow dots), and is promoted to a confirmed track.
    Noisy detections (white) are associated with the track (green lines)
    and eventually, the track converges to the target's true state.}
    \label{fig:track-visual2}
\end{figure}

\subsection{Pipeline Ablation Study}
An ablation study is performed on the desktop computer 
to determine the contribution of
each stage to the overall latency of the pipeline and their
marginal effects on its throughput. Latency is measured as 
the time between the arrival of a chirp to the capture stage of
the processing subsystem, and the
generation of the output of the pipeline, whatever it may be. 
Throughput is measured as the number of frames processed per 
second after processing 1000 frames. Dataset DS2 simulates the 
receiver signals of targets across a wide variety of ranges in
high noise. The pipeline is run on the data. On each trial,
a stage is added. The results are depicted in Figures
\ref{fig:ablation-cumulative-latency}, \ref{fig:ablation-latency-breakdown}, and \ref{fig:ablation-throughput-segments}.

\begin{figure}
    \centering
    \incfig[1]{Ablation_Cumulative_Pipeline_Latency}
    \caption{Cumulative Pipeline Latency per Frame, split between
    the processing subsystem "tinysar-bat-redux" and the 
    detection and tracking subsystem "tinysar-data". "Segments"
    refers to the number of integrated output chirps for every 
    256 input chirps.}
    \label{fig:ablation-cumulative-latency}
\end{figure}

From the figure depicting the cumulative latency of 
each of the pipeline stages (Figures \ref{fig:ablation-cumulative-latency} 
and \ref{fig:ablation-latency-breakdown}), it can be seen that the latency
of the pipeline is highly dependent on the degree to which the input 
chirps are summed together in the integration stage of the processing
subsystem. Notably, the integrate stage contributes the most time to 
the pipeline latency when it is doing the least work (no integration,
outputting 256 chirps for every 256 input chirps). Figure 
\ref{fig:ablation-latency-breakdown} illustrates that the processing
application is mainly invariant to the number of segments, while 
the detection and tracking subsystem's latency increases 
proportionally with it. As a result, the first stage is faster 
than the second for segment counts above 4 and vice versa for
segment counts below 4. 

\begin{figure}
    \centering
    \incfig[1]{Ablation_Latency_Breakdown}
    \caption{Pipeline Latency Breakdown as stages are added for 
    different integration segment counts (number of output chirps for 
    every 256 inputs chirps). Split into the processing subsystem
    "tinysar-bat-redux" the detection and tracking subsystem, 
    "tinysar-data", and a combined view.}
    \label{fig:ablation-latency-breakdown}
\end{figure}


\begin{figure}
    \centering
    \incfig[0.8]{Ablation_Dat_Throughput_Segments}
    \caption{Pipeline Throughput per Frame as stages are added, for 
    different integration segment counts (number of output chirps for 
    every 256 inputs chirps). Obtained by finding average frames 
    per second after processing 1000 frames each with 256 input chirps.}
    \label{fig:ablation-throughput-segments}
\end{figure}

While latency is generally between 20 and 50 ms for segment counts 
less than 64, the throughput of the pipeline is greater than 
5000 frames a second, or 0.2 ms per frame. This 
two-orders-of-magnitude difference between latency and 
throughput is due to the fact that multiple frames are processed 
simultaneously by the pipeline. While memory is available, a 
thread will be spun up to process each incoming frame.

\subsection{Tracking performance on real-world data}
The radar hardware was taken to an open air field test site in New South Wales 
where it and a target drone were flown in a variety of scenarios. Figure 
\ref{fig:real-world-tracking} depicts one such air to air scenario. 
\begin{figure}
    \centering
    \includegraphics[width=0.8\textwidth]{air-to-air.png}
    \caption{The radar is flown into the air and is pointed at a target drone carrying
    a reflective marker 200m away.}
    \label{fig:real-world-tracking}
\end{figure}

The results of the tracking subsystem failed to produce any tracks of the ariel target, 
due to high electrical noise at the beginning of each pulse that masked the close field
where the target was. Fortunately, the radar detected high cross-section targets 
in the distance. This is illustrated in the visualisation of the angle sensing stage
in Figure \ref{fig:points-over-map}, where detections from the radar line up with
a silo and trees.    

\begin{figure}
    \centering
    \incfig[0.8]{points-over-map-singleton}
    \caption[Field Trial Silo Detection]{The detected points from the radar are plotted 
    over a map of the area. There is a high correspondence between detected points and
    the three main high cross-section targets in the area: a silo 603 meters away, 
    building 706 m away, and trees surrounding a building 410 meters away. }
    \label{fig:points-over-map}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=0.6\textwidth]{field-and-silo.png}
    \caption[silo]{A silo in the distances and a property surrounded by trees are large 
    visible targets for the radar when it is airborne.}
    \label{fig:field-and-silo}
\end{figure}

\FloatBarrier
\input{Sections/Ch5 Results/Processing Subsystem.tex}


\input{Sections/Ch5 Results/Tracking Subsystem.tex}


