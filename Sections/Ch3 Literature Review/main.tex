\chapter{Literature Review}
% link to graph https://app.litmaps.com/shared/697576cb-ab15-4656-b20a-59ad337a9664
\label{ch:literature_review}

\section{Miniature Compact Airborne Radar for UAV DAA}
The ASTM F3442/F3442M-23 standard outlines practical requirements for 
autonomous \gls{daa} systems. While the safe range from other 
aircraft is dependent on its type, speed, and whether it is cooperative 
with an \gls{adsb} system, the minimum range required is in no case 
less than 2,000 feet (609.6 m) horizontally and 250 feet (76.2 m) 
vertically \cite{astm_f3442_2023}. This forms a cylindrical volume 
referred to as the "well clear zone". On top of this, a DAA system 
should detect targets well in advance. Using the F3442 35 second 
lead time as a guide, a radar would need to have a range of 
3500 metres in order to detect and avoid a target travelling at 
100 metres per 
second \cite{Wang2016}. 
Methodologies for 
producing detections on this scale 
each have advantages and limitations \cite{Yasin2020}. 

Radar enables long-range detections of small 
targets in all weather \cite{9635901} and provides direct range and velocity 
measurements \cite{skolnik2001radar}, which are critical for collision avoidance.
While radar has been previously too heavy and power-hungry for small UAVs 
\cite{Lies2020, McLain2015, Wang2016}, papers from as early as 2006 and 
2009 outline 
radar-based solutions with awareness of their miniaturisation potential 
\cite{4148337, 5438372}. 
The work of Sysak et al.\ % Reorder list
\cite{sysak2025_lightweight_compact_pulse_radar_uav_collision_avoidance},
Wang et al.\ \cite{Wang2016}, and Moses et al.\ \cite{6044363, 
uavborne_moses_2011, Moses2015} 
demonstrate this feasibility, but important problems remain before radar 
\gls{daa} can be widely adopted.

Some of the first radar systems tested on small (<25 kg) \gls{uav}s were
shown in a set of feasibility studies by Moses, Rutherford, and Valavanis 
published in 2011 \cite{6044363, uavborne_moses_2011} that validate a 
miniature X-band radar system. One of the studies
\cite{6044363} mounts the 230 gram radar on a small quadcopter (see 
Figure \ref{fig:moses-quadcopter}), and successfully demonstrated target
detection and identification in a laboratory environment. Their 
evaluation emphasises target identification via micro-Doppler signatures rather
than ranging or tracking, achieving a 100\% detection rate at 7 metres from
the target. When distance increased and both platform and target took to the
air, accuracy dropped significantly --- a result that highlights the importance
of validating radar pipelines under realistic encounter conditions. The other
study outlines the post-detection decision making and avoidance manoeuvre as 
a control problem, but doesn't conduct airborne tests to validate the system 
in realistic conditions. While the works together outline the software pipeline 
from end to end, they do not 
characterise individual stage performances in areas such as latency or 
throughput, nor do they compare alternative algorithmic choices.

A follow-up 2014 publication by this group (Moses et al.\ \cite{Moses2015})
presents an even smaller radar (see Figure \ref{fig:moses-2014}), now with 
angle sensing and collision avoidance algorithms. The system is
evaluated indoors against large reflective targets. While the collision
avoidance algorithms are compared thoroughly, no other algorithm choices are
evaluated and readiness for open-air tests was not demonstrated --- the authors
themselves note that higher transmit power would be needed to detect targets 
at the ranges for safe UAV operation. 

\begin{figure}[htbp]
    \centering
    \begin{minipage}{0.44\textwidth}
        \centering
        \includegraphics[width=\linewidth]{Moses 2013 quad copter.png}
        \caption{Moses et al.'s miniature radar mounted on a small quadcopter \cite{6044363}.}
        \label{fig:moses-quadcopter}
    \end{minipage}
    \begin{minipage}{0.4\textwidth}
        \centering
        \includegraphics[width=\linewidth]{moses-2014.png}
        \caption{Second generation of Moses et al.'s miniature radar, with
        angle sensing capabilities \cite{Moses2015}.}
        \label{fig:moses-2014}
    \end{minipage}
\end{figure}

% Spencer, Newmeyer, Fasano, Yasin and Wang all reference Moses 
% Shi and Itcia performed airborne tests on fixed winged class 2 UAVs
Shi et al. \cite{6712628}, Itcia et al. \cite{fmcw_itcia_2013} 
and Fasano et al. \cite{Giancarmine2016} each demonstrate airborne 
radar tracking on group 3 (greater than 25 kg) UAVs of group 2 
(greater than 10 kg) UAVs at kilometre ranges, filling gaps in 
Moses et al.'s work, but with a larger radar unsuitable for smaller UAVs. 
Wang et al.\ 
\cite{Wang2016} builds upon Moses et al.'s work through the 
design and simulation of a similarly sized miniature radar with  
ranging capabilities similar to those of Shi et al. and Itcia et al. The 
authors design a low-SWaP \gls{lfmcw} system
for group 2/3 (greater than 10 kg) \gls{uas} incorporating \gls{fft}-based 
detection, digital
beamforming, and a GPU-accelerated \gls{fft} for a high frame rate. The
research establishes the feasibility of ranging targets beyond 5 km through
beamforming for miniature radar, but angle sensing is not explored, performance 
is validated in
simulation only, and compute and latency constraints are not considered.

Spencer directly builds on the works of Moses et al.\, Shi et al.\, Itcia et al.\, 
and Fasano et al.\ by designing a compact four-element 250 gram phased array radar 
system that meets the SWaP constraints of a \gls{uav} less than 25 kg in mass 
\cite{compact_spencer_2015} (see Figure \ref{fig:spencer}).
Experiments reveal that the radar can detect targets of less than 0.1 m$^2$ 
cross-section at ranges of 150 metres. This makes the work one of the first
to demonstrate feasibility on this scale. The work found heavy 
clutter caused by electromagnetic interference, and demonstrated 
the use of the Levinson--Durbin algorithm to generate filter coefficients
\cite{levinson1947wiener, durbin1960fitting} 
to suppress static clutter, and the Recursive Random Sample
Consensus Algorithm (R-RANSAC) to maintain an accurate 
world model in the presence of clutter. The system block 
diagram is depicted in Figure \ref{fig:spencer dsp}, and 
resembles the pipeline of this thesis. 

% Spencer does cfar after angle sensing to enjoy higher SNR
% as a consequence, he needs to apply an arbitrary threshold 
% to beam-forming to save on compute. We apply a technique 
% that allows us to apply CFAR before angle sensing without
% reducing detection sensitivity.  

\begin{figure}
    \centering
    \begin{minipage}{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{spencer radar.png}
        \caption{Spencer et al.'s miniature radar system with a mass of 250 grams. \cite{compact_spencer_2015}.}
        \label{fig:spencer}
    \end{minipage}\hfill
    \begin{minipage}{0.52\textwidth}
        \centering
        \includegraphics[width=\textwidth]{spencer dsp.png}
        \caption{Spencer's digital signal processing pipeline block diagram \cite{compact_spencer_2015}.}
        \label{fig:spencer dsp}
    \end{minipage}
\end{figure}

Mohammadkarimi et al.\ \cite{Mohammadkarimi2024} support the feasibility of
electronically scanning to measure azimuth and elevation of targets, but
similarly validate ranging and angle estimation in simulation only. While the
work is mathematically rigorous in its treatment of \gls{daa} requirements,
empirical validation of the software pipeline under UAV SWaP constraints is
absent.

% Fiorio does a really good camera-radar sensor fusion, maybe mention in the conclusion. 
% pointed radar because of computational constraints. 
% Maybe mention echodyne

Lies et al.\ across two studies in 2020 and 2021 aim to support the adoption 
of radar DAA by small UAVs by extending the range of commercially available, 
low-\gls{swap} radar chipsets \cite{Lies2020, 9380893}. Rather than have the radar 
scan for targets, their technique uses a camera to supply a target's azimuth 
and elevation, so that the radar processes returns from only that direction; 
this delegation relaxes the computational load and frees measurement time for 
SNR-increasing techniques such as long dwell intervals, coherent integration, 
and phase-drift and range-bin-migration correction. Using a 77~GHz development 
kit, the authors detect a small hovering drone at 120~m --- a range limited by 
flight-altitude regulations rather than the radar --- and, through a 
link-budget extrapolation, argue that a large VTOL-sized aircraft would be 
detectable at approximately 1~km under the same hardware \cite{9380893}. 
Notably, the 2021 study compares four range-estimation algorithms 
(FFT, discrete polynomial transform, noncoherent integration, and spectral 
entropy), one of the few works to treat algorithm choice as a design variable. 
However, this comparison is confined to a single pipeline stage 
(range estimation), evaluated in simulation against a single target under white 
Gaussian noise, with computational metrics such as latency and throughput
considered but not measured, and without any treatment of angle 
estimation, detection, or tracking.

Sysak et al.\
\cite{sysak2025_lightweight_compact_pulse_radar_uav_collision_avoidance} (2025)
present a lightweight X-band pulse radar for mid-air collision avoidance on
medium UAV platforms, using a fixed forward-looking antenna and FPGA-based pulse
compression. In a ground-based field campaign, the radar reliably detected large
static urban structures at 1--3\,km with a 1.5\% false-alarm probability, though
detection is characterised almost exclusively against these large, high-RCS
targets under low, spatially uniform clutter using a single CA-CFAR detector,
leaving the sensitivity floor against small moving airborne targets
uncharacterised. A separate trial detected a 3.15\,m-wingspan UAV at 6.4\,km
(see Figure~\ref{fig:sysak}), demonstrating kilometre-scale detection within a
mediumaUAV power budget, but at markedly lower peak-to-background ratio and
without systematic sensitivity statistics. The validated system performs range
estimation only, deferring angle sensing to a future upgrade, and although a
range-rate tracker and \gls{tcpa} logic are described, no tracking-quality or
latency results are reported.

\begin{figure}
    \incfig[0.9]{sysak_radar}
    \caption{Sysak et al.'s experimental setup and results. The radar is
    capable of detecting large targets at 1--3 km with high ranging accuracy.
    We observe peaks in amplitude over range that correspond to target position
    \cite{sysak2025_lightweight_compact_pulse_radar_uav_collision_avoidance}.}
    \label{fig:sysak}
\end{figure}

Taken together, these works establish that radar hardware small and low-power
enough for \gls{uav} \gls{daa} is feasible, shifting attention towards the
design of software pipelines suitable for the compute constraints and detection 
requirements of \gls{daa} systems.

\section{GPU-based Radar Processing Under SWaP Constraints}

Historically, literature that explores the software pipelines for radar 
processing on embedded platforms have focused on \gls{fpga}s to achieve
high performance with low power and weight. Shi et al.\ \cite{6712628} 
describe a small \gls{fmcw} phased array built from off-the-shelf development 
boards, validating that continuous real-time 2D FFTs are achievable on a 100 
MHz FPGA. However, processing was split between an onboard FPGA and offboard 
post-processing, and signal coherence problems prevented real-world validation. % kinda related to the problem that Lies solves
Newmeyer et al.\ \cite{7577322} go further, demonstrating a 120 gram, 8 Watt 
\gls{fmcw}
phased array pipeline on a Zynq ARM System-on-Chip. Through closely monitoring 
power, latency
distributions, and hardware resources the work establishes that embedded 
compute platforms can run radar processing algorithms in real time, even 
under strict SWaP constraints. Only one hardware
configuration and one algorithm sequence are evaluated. 

The efficiency of FPGAs comes at the cost of long development cycles, and 
hardware description expertise. The maturation of high level parallel 
programming frameworks such as \gls{cuda} and the arrival of small but 
powerful embedded GPU modules such as NVIDIA's Jetson line, has made 
\gls{gpu}s a practical alternative to \gls{fpga}s for 
embedded radar processing. This is reflected by a growing body of work 
that demonstrates the feasibility of GPU acceleration for radar processing. 

Zhao et al.\ \cite{rs15225421} implement CUDA acceleration for a passive
bistatic radar pipeline spanning clutter suppression, range--Doppler
processing, and CA-CFAR detection, reporting a maximum acceleration ratio of
113.13$\times$ over a CPU baseline, but omit angle estimation, coordinate
transforms, and tracking, and do not discuss memory reallocation or thread
synchronisation costs. Fang et al.\
\cite{Fang2024} cover more of the chain, including \gls{mti}, \gls{mtd}, clutter map
detection, point fusion, and trajectory tracking. The design achieves an 11$\times$
speedup over a CPU implementation without loss of track quality (see Figure
\ref{fig:fang_pipeline}), but only evaluates a single platform and pipeline configuration. 
Mondal et al.\ \cite{Mondal2024_gpu_radar} provide the
closest treatment of implementation detail, analysing memory-access and
parallelism optimisations per stage across \gls{mti}, Pulse Doppler Processing,
and CA-CFAR, but their evaluation is confined to a single desktop-class NVIDIA
Quadro FX~1800, architecturally distinct from the Jetson-class edge platforms
typical of autonomous drones, and to a single pipeline configuration. Zaknoun
\cite{zaknoun2024_realtime_gpu_accelerated_radar_processing} treats GPU radar
software architecture for real-time processing in depth, but outside a UAV DAA
context and without evaluation under UAV SWaP constraints. The pipeline lands
packets of raw radar data directly onto the GPU, bypassing CPU staging and 
host-device transfer, and finds a bottleneck in CPU-side post-processing. 
% Refer to that last finding in the discussion

\begin{figure}
    \includegraphics[width=0.7\textwidth]{fang-dataflow.png}
    \caption{Fang et al.'s GPU radar processing pipeline \cite{Fang2024}}
    \label{fig:fang_pipeline}
\end{figure}

\section{Sensitivity and Detection in Low-SNR Regimes}

An et al.\
\cite{an2024_time_varying_angle_estimation_unresolved_extended_targets_monopulse}
propose an efficient method for resolving two unresolved targets within a
single monopulse cell, but evaluate it only under simulated white-noise
conditions without integration into an end-to-end pipeline. Explicit failure
modes exist for same-angle targets and cells containing three or more targets.

Aubry et al.\
\cite{aubry2020_single_pulse_simultaneous_detection_angle_estimation_multichannel}
demonstrate that detection and angle estimation can be treated jointly in a
multichannel array, increasing sensitivity to targets not visible in any single
channel and avoiding brittleness of early thresholding under low SNR. Validation
is limited to single simulated targets in Gaussian interference.

Ezuma et al.\ \cite{ezuma2019microuav} demonstrate that Gaussian and Rayleigh
clutter assumptions --- the basis of CA-CFAR --- already fail for ground-based
mmWave radar detecting UAVs, finding that a Weibull distribution provides a
significantly better fit to measured clutter statistics, and explicitly noting
that prior UAV detection work had neglected this. This finding directly 
undermines the Gaussian noise assumption of the CFAR algorithm and 
the noise assumptions of many of the simulation-based studies. This leaves
the selection of an appropriate detection algorithm an open question that 
is investigated in this work. The airborne 
case introduces
further departures from the homogeneous Gaussian noise model: a vibrating
platform subject to altitude variation, aspect angle changes, and
platform-induced interference all corrupt the assumptions underpinning standard
windowing and CFAR algorithm selection. No published work has measured or
modelled these statistics for a miniature airborne FMCW radar in real flight
conditions.

Existing angle estimation and detection algorithms have therefore been
validated only under idealised noise conditions that are unlikely to reflect
the environment experienced by a miniature airborne radar.

\section{Summary and Research Gap}

Across the three areas surveyed above, the literature makes meaningful
progress in three directions. Compact radar hardware has been demonstrated on
platforms as small as a quadcopter \cite{6044363, Moses2015}, with some works
achieving detection ranges beyond 5 km \cite{Wang2016}. GPU and embedded
compute platforms have been shown capable of accelerating the core stages of a
radar signal processing chain by an order of magnitude or more
\cite{Fang2024, rs15225421, Mondal2024_gpu_radar}. And angle estimation
algorithms capable of resolving closely-spaced or low-SNR targets have been
proposed \cite{an2024_time_varying_angle_estimation_unresolved_extended_targets_monopulse,
aubry2020_single_pulse_simultaneous_detection_angle_estimation_multichannel}.

However, the field consistently falls short in three areas. First, no work
validates a complete sensing-to-tracking pipeline under realistic airborne
encounter conditions against small moving targets. Hardware demonstrations
either use ground-based setups, large reflective targets, or post-processed
data \cite{sysak2025_lightweight_compact_pulse_radar_uav_collision_avoidance,
6044363, Moses2015}. Simulation-only studies make no attempt at empirical
validation \cite{Wang2016, Mohammadkarimi2024}. As a result, the relative
merit of competing algorithm choices for CFAR, angle estimation, and tracking
has not been assessed under the (non-Gaussian \cite{ezuma2019microuav}) 
noise conditions and target characteristics
representative of small UAV encounters, leaving algorithm selection for this
regime without empirical grounding. Second, while GPU acceleration studies
demonstrate large aggregate speedups, they do not treat the implementation of
algorithms as a design variable. No work systematically compares
implementations with different parallelisation strategies, memory layouts, or
host--device transfer patterns. These choices directly determine the
latency, throughput, and memory requirements of the pipeline. Third, existing
GPU studies evaluate a single hardware platform, typically a desktop GPU
\cite{rs15225421, Mondal2024_gpu_radar} or a non-UAV embedded system
\cite{zaknoun2024_realtime_gpu_accelerated_radar_processing}. Several 
of the works surveyed acknowledge that algorithm choice is driven by available 
compute. Spencer applies an arbitrary 
threshold before angle sensing to reduce computational load, and Fiorio et al.
utilise a camera to perform some detection work to find areas of interest 
in the radar data. No work systematically examines 
the effect that hardware selection has on the range of viable algorithms 
and implementations. Given that embedded GPU
architectures differ substantially from desktop GPUs in memory bandwidth,
parallelism, and thermal constraints, conclusions drawn from one platform
cannot be assumed to generalise.

This thesis addresses these gaps by characterising detection and tracking
sensitivity against small moving airborne targets across a representative
selection of algorithm choices, by systematically comparing low-level
implementations of those algorithms and quantifying their effects on
end-to-end throughput and latency, and by evaluating how both sets of
conclusions vary across embedded hardware configurations to produce
hardware-aware design recommendations.